<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"

       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

    <context:component-scan base-package="com.dz.shiro"/>

    <bean id="securityManager" class="org.apache.shiro.web.mgt.DefaultWebSecurityManager">
        <property name="realm" ref="shiroDbRealm" />
        <property name="cacheManager" ref="cacheManager" />
    </bean>
    <!-- 項目自定义Realm -->
    <bean id="shiroDbRealm" class="com.dz.shiro.ShiroDbRealm" />
    <bean id="cacheManager" class="org.apache.shiro.cache.MemoryConstrainedCacheManager" />
    <bean id="lifecycleBeanPostProcessor" class="org.apache.shiro.spring.LifecycleBeanPostProcessor" />

    <!-- Shiro Filter -->
    <bean id="shiroFilter" class="org.apache.shiro.spring.web.ShiroFilterFactoryBean">
        <property name="securityManager" ref="securityManager" />
        <property name="loginUrl" value="/user/login2" />
        <property name="successUrl" value="/user/main" />
        <property name="unauthorizedUrl" value="/403" />
        <property name="filterChainDefinitions">
            <value>
                /user/tologin= anon <!--放行-->
                /user/update = perms[修改]<!--请求的url 是 /adduser 当前的登录者要具有adduser的权限-->
                /static/**=anon<!--static 静态资源放行-->
                /** = authc<!--所有的请求都要经过认证-->
            </value>
        </property>
    </bean>


</beans>